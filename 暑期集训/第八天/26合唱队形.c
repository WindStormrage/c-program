#include<stdio.h>
int main()
{
 int n,t[231],c;
 int max,a,b;
 scanf("%d",&n);
 for(a=0;a<n;a++)
    scanf("%d",t[a]);
 for(a=0;a<n,a++)
    {
     t[a];
     for(b=a;b<=a;b++)
     for(b=a;b<n;b++)
    }
 system("pause");
 return 0;
}
